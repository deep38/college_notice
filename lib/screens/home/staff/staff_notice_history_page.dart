import 'package:flutter/material.dart';

class StaffNoticeHistory extends StatelessWidget {
  const StaffNoticeHistory({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}